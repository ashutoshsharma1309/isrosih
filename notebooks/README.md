# notebooks/

Exploratory data analysis and model experiments (Phases 2-5).

Rules:
- Notebooks are for exploration; anything that becomes part of the product
  is promoted to a proper module (`ai_models/`, `satellite_processing/`).
- Name notebooks `NN_topic.ipynb` (e.g. `01_rainfall_eda.ipynb`) so the
  analysis sequence is readable.
- Clear large outputs before committing.
